# Case_Study
This repo contains Crash_Investigation_Framework

Steps to run Crash_Investigation_Framework :
  1. clone this repo
  2. run command --> cd Crash_Investigation_Framework && spark-submit --master local  carCrashInvestigation.py  --files config_files Utils
  3. outputs will get saved in --> /Crash_Investigation_Framework/output
