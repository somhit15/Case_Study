# Case_Study
This repo contains Crash_Investigation_Framework



Steps to run Crash_Investigation_Framework :
  1. clone this repo
  2. run command --> cd Case_Study && cd Crash_Investigation_Framework && spark-submit --master local  carCrashInvestigation.py  --files config_files Utils_General
  3. outputs will get saved in --> /Case_Study/Crash_Investigation_Framework/output
  4. Outputs of all the question will get printed in console while running spark-submit command
  
  
